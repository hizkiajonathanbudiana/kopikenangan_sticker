# Sticker Drop Zone

Each template reads its assets from a dedicated folder:

- `public/stickers/kawung/`
- `public/stickers/mega/`

Keep the filenames in sync with `src/data/stickerLab.ts` so the preview grid and checklist mirror the files you plan to upload to LINE Creators Studio.
